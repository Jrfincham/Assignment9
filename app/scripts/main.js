var containermain = $('.maincontent'),
  img_url,
  img_tag;

items.results.forEach( function(item){

  img_url = item.Images[0].url_fullxfull;
  img_tag = itm src ='" + img_url + "'>;
});

 // $('maincontent').append(img src="' + item.images[0].url_fullxfull +'">');
